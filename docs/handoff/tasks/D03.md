# D03 — Restore onto a clean local destination

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

D02, C01

## Read first

- `full recovery-set format from D02`
- `ridge/preflight.py`
- `ridge/schema.py`
- `provider inventory contract`
- `docs/expanded-operations.md`

## Implement

Implement restore to fresh event-scoped volumes/desktops using the exact release. Restore native IDs/receipts before binding core mappings. Rebind only provider endpoints and runtime secrets; restore released evidence generation, index state, desktop cases and team workspaces. Leave destination paused. Refuse a wrong release, corrupt backup or already-live destination.

## Acceptance checks

After wiping only disposable test resources, restore and compare scores, solved answers, owners/generations, IRIS findings, pending deliveries, index records and workspace hashes. Retry restore without duplicates. Resume and solve one more question successfully. Capture measured recovery time.

## Do not expand scope

No production overwrite, partial-ID remapping shortcut or cross-site activation yet.

## Finish and stop

Review: **mandatory restore review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
