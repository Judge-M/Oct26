# A05 — Fix the Autopsy evidence-path and preflight contract

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

A03

## Read first

- `expanded/author.py`
- `ridge/preflight.py`
- `expanded/prepare.py`
- `assets/autopsy-case-v2.json`
- `deployment/expanded/desktop/configure-image.sh`
- `tests/test_resilience.py`

## Implement

Separate an evidence reference from a tool’s local case entrypoint. Autopsy opens ~/Cases/WS17/WS17.aut; source files remain at /evidence and originals at /originals. Make controller preflight verify real released source inventory and desktop readiness verify the writable case/template. Eliminate the nonexistent /evidence/autopsy/WS17/WS17.aut requirement. Document backward compatibility for generated tickets.

## Acceptance checks

Generate a release from current source and run preflight against stubbed remote identities with the real filesystem layout. It passes without a shared writable case. Missing source or desktop case still fails the appropriate check. Ticket navigation names the actual installed path.

## Do not expand scope

Do not create an empty .aut placeholder, disable source validation, or regenerate native evidence.

## Finish and stop

Review: **standard**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
