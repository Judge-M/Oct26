# D01 — Test live transactional failures and pause races

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

B05, C03

## Read first

- `ridge/state.py`
- `ridge/service.py`
- `ridge/transport.py`
- `integrations/iris_silent_ridge.py`
- `integrations/ctfd_silent_ridge/__init__.py`
- `tests/test_resilience.py`

## Implement

Extend the real-service harness with a response-dropping proxy and process restarts. Force remote commit then lost response, simultaneous duplicate deliveries, claim/relinquish with stale generations, worker death mid-lease, Wazuh failure during follow-up publication, and pause/export races. Verify unrelated tickets can proceed while a destination fails.

## Acceptance checks

For each accepted question: one award, one finding and stable receipt key after retries; tasks/closures respect dependencies. Stale owner submissions fail. Nothing participant-controlled mutates before start/while paused. No network I/O holds the SQLite transaction. Bound recovery time and assert no swallowed failures.

## Do not expand scope

Do not alter receipt keys to make tests pass, bypass real adapters or promise active-active support.

## Finish and stop

Review: **mandatory transaction review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
