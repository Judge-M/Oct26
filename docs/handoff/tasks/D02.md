# D02 — Build coherent full-event backup

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

B05, C02, C03, A04

## Read first

- `ridge/export_run.py`
- `ridge/state.py export_barrier`
- `ridge/storage.py`
- `central and Guacamole Compose volumes`
- `desktop runtime inventory`

## Implement

Add a recovery-set command that pauses/drains, freezes native edits and desktop writes as needed, creates native IRIS/CTFd/Guacamole DB backups, core SQLite-consistent backup, Wazuh snapshot, released evidence and per-team case/workspace backups. Include private identity/secret restoration material encrypted separately from public artifacts. Record schema/version/resource mapping and receipt watermarks; publish completion marker last.

## Acceptance checks

Inject a failure in every phase: no incomplete set is marked restorable. Restore-required components and encryption key references are enumerated. Receipt/score counts and case/workspace hashes are captured. Backup does not require new Internet downloads and resumes/releases barriers safely after failure.

## Do not expand scope

Do not treat logical JSON audit export as a disaster backup or copy a live SQLite file unsafely.

## Finish and stop

Review: **mandatory backup review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
