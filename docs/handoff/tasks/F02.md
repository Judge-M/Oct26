# F02 — Automate local↔AWS switching with rollback

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

F01, D03, E04, E05

## Read first

- `ridge.deploy switch interface`
- `backup/restore operations`
- `active-site fence`
- `endpoint configuration and desktop evidence generation`

## Implement

Compose destination preparation, source pause/drain, coherent backup, source fencing, restore, readiness, access-endpoint switch and controlled resume. Support both directions. Verify old local paths/IPs are not treated as incident evidence changes. Reuse cloud/local golden images and restore only event state. Produce a recovery point/time report.

## Acceptance checks

During a disposable active run, move local→AWS→local with scores, findings, owners, releases and team workspaces preserved. Inject failure at each phase and demonstrate rollback or safe paused recovery. Source-unreachable path uses the last completed backup and reports possible lost work rather than claiming full continuity.

## Do not expand scope

No automatic restart of the old site after destination activation; no hidden full-data deletion.

## Finish and stop

Review: **mandatory recovery review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
