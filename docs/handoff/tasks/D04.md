# D04 — Bound storage growth and implement safe retention

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

D02, D03, A04

## Read first

- `ridge/storage.py`
- `ridge/evidence_release.py`
- `deployment/expanded/measure.py`
- `Compose logging/volumes`
- `recovery-set inventory`

## Implement

Extend storage accounting to VM clones, cases, workspace files, app logs, indices, backups, partial downloads and release caches. Estimate incoming allocation before copies/imports and enforce configurable reserves. Add dry-run cleanup scoped to event-owned IDs, removing only expired, independently verified backed-up data; preserve golden artifacts and active runs.

## Acceptance checks

Low-space tests stop before partial publication. Unknown directories/resources are never removed. Corrupt/missing backup prevents protected pruning. Abandoned staging cleanup runs only when no publisher holds its lock. Repeat reset/backup cycles show bounded retained growth under the configured policy.

## Do not expand scope

No systemwide prune, arbitrary recursive deletion or silent deletion of incomplete backups.

## Finish and stop

Review: **mandatory deletion review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
