# E05 — Implement AWS expiry and verified teardown

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

E04, D03, D04

## Read first

- `AWS provider resource inventory`
- `recovery-set receipts`
- `event retention/TTL profile`
- `ridge.deploy down contract`

## Implement

Implement down with a completed restorable backup prerequisite, plus an approved emergency/TTL policy that cannot leave uncontrolled compute running. Delete only tagged and journal-owned resources. Inventory EC2, EBS, IPs, import staging, snapshots, AMIs, logs and any network services; distinguish retained backups/images from leaks. Store teardown evidence outside the destroyed host.

## Acceptance checks

Destroy an active disposable event and an interrupted deployment. Re-run safely. Verify no event compute or unintended network/storage resource remains. Print retained resources and continuing cost categories; stopped EC2 is not reported as zero cost. Simulate failed backup and expired credentials with explicit safe outcomes.

## Do not expand scope

No accountwide delete or removal of the only verified recovery set. Spending/retention limits must be set first.

## Finish and stop

Review: **mandatory cost/deletion review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
