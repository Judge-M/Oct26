# E02 — Provide the Windows one-command local launcher

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

E01, C01, C02

## Read first

- `ridge.deploy interface from E01`
- `local provider from C01`
- `docs/desktop-image.md`
- `private profile schema`

## Implement

Add a thin ridge.ps1 launcher that checks supported PowerShell/Python/Hyper-V prerequisites, validates the requested profile, and invokes the shared CLI with correct quoting and exit codes. Preparation installs/caches required dependencies only under the approved host envelope. Produce a private credential handout and stable local access URL after readiness.

## Acceptance checks

From a clean supported Windows preparation/event host, execute prepare then one up command without manual shell edits. Test spaces in paths, unavailable elevation, missing virtualization, interrupted download and rerun. No host reboot/install happens unexpectedly on event day. Offline prepared setup makes zero outbound calls.

## Do not expand scope

Do not promise a blank unprepared Windows machine can import gigabytes instantly; do not put private passwords in command arguments.

## Finish and stop

Review: **standard**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
