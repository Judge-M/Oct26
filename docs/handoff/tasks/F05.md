# F05 — Publish the complete versioned offline release

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

E02, E04, D03, C04, F04

## Read first

- `ridge/distribution.py`
- `ridge/bundle.py`
- `ridge/artifacts.py`
- `.github/workflows/publish.yml`
- `expanded/manifest.example.json`
- `all artifact manifests`

## Implement

Unify distribution and complete-offline inventory semantics without breaking old downloads. Pin/build/publish custom GHCR images and every upstream image, include guides/cert bootstrap dependencies and both provider image references, and save exact offline archives in bounded parts. Verify anonymous or intended authenticated package access. Make source installation/materialization automatic and atomic; do not leave users copying asset-NNNN.bin files manually.

## Acceptance checks

Publish a candidate release only after matching source/image checks. Cold-cache fetch and install reconstruct all files and verify hashes; LFS stubs, missing parts, wrong image/source, low disk or interruption fail cleanly. Run a no-Internet local install. Capacity/restore gates must be linked before setting compatibility_verified/event_ready.

## Do not expand scope

No placeholder digests, mutable latest tags, or downloaded-but-untested completeness claims. Do not publish live secrets/participant state.

## Finish and stop

Review: **focused release review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
