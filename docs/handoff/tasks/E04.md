# E04 — Implement minimal AWS infrastructure and provisioning

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

E01, E03, B04

## Read first

- `ridge.deploy provider interface`
- `private AWS profile`
- `B04 networking contract`
- `E03 AMI inventory`

## Implement

Use one checked-in infrastructure definition/tool and a thin Python adapter; avoid overlapping Terraform and ad-hoc creation code. Provision tagged central EC2, team desktop EC2, networks/security groups, encrypted storage, approved access endpoint and narrowly scoped roles. Pin image IDs, resolve real addresses, and reuse shared application bootstrap. Separate retained image/backup resources from event compute.

## Acceptance checks

Dry-run lists exact resource types/counts and budget assumptions. Two-team create/rerun/interrupted-create/cleanup tests succeed without duplicate instances. No public database/indexer/VNC or integration API. Expand config to ten teams without manual IP edits. Record actual resource inventory and service readiness.

## Do not expand scope

No Kubernetes, mandatory NAT gateway, managed DB expansion or fabricated instance pricing; obtain current estimates only in the authorized region.

## Finish and stop

Review: **focused infrastructure review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
