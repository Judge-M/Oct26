# A03 — Define and validate one private deployment profile

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

None; may start after implementation is authorized.

## Read first

- `expanded/config.json`
- `ridge/preflight.py`
- `deployment/expanded/compose.central.yaml`
- `deployment/expanded/compose.guacamole.yaml`
- `ridge/scenario.py`

## Implement

Add a versioned deployment profile schema and validator in a focused ridge/deploy configuration module. Include event/release IDs, provider, ten-team roster shape, host capacity, addresses, secret references, retention and spending/TTL envelope. Keep incident_date separate from event_start. Separate desired names from resolved IDs in generated runtime inventory. Generate neutral accounts when no roster is provided.

## Acceptance checks

Tests reject duplicate names, unmapped desktops, documentation-only production addresses, missing required fields, invalid dates, incompatible versions and secrets embedded in public examples. A two-team and a ten-team profile validate without any fixed application IDs. Error messages name the exact field.

## Do not expand scope

No provisioning or credentials in Git. Freeze the schema before dependent agents edit consumers.

## Finish and stop

Review: **standard**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
