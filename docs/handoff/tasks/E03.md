# E03 — Prepare and validate the AWS desktop AMI

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

A03, A04, C01

## Read first

- `assets/desktop-v1.json`
- `docs/desktop-image.md`
- `desktop sealing/configuration scripts`
- `AWS official VM Import prerequisites`

## Implement

Within the approved AWS account/region and budget, convert the source to a supported import format, stage it privately and import a versioned AMI. Test boot mode, ENA/NVMe/device naming, cloud-init, filesystem growth, guest identity and all tools. Tag source hash/release and save AMI/import IDs in the preparation inventory. Cache by hash so event day does not repeat import.

## Acceptance checks

Launch one disposable imported desktop, provision runtime configuration, reopen the case/search and compare evidence hashes. Stop/recreate successfully and inspect actual root-volume size. Confirm image has no build keys/VNC secrets. Clean the test instance/import staging under retention policy.

## Do not expand scope

No claim that a QCOW2 conversion alone makes a usable AMI; no paid launch without the organizer envelope.

## Finish and stop

Review: **focused cloud image review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
