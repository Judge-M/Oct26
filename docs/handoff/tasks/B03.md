# B03 — Vendor and bootstrap the complete Wazuh historical stack

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

A03

## Read first

- `deployment/expanded/versions.json`
- `ridge/evidence_release.py`
- `ridge/preflight.py`
- `ridge/scenario.py`
- `docs/expanded-deployment.md`

## Implement

Vendor a version-pinned official Wazuh Compose/configuration set with provenance and image identities. Automate local CA/cert creation, integration writer restricted to the event index, participant read-only identities, initial indexing and saved views. Provide timestamped and timeless views; coverage/catalog facts must not disappear under a date filter. Configure explicit CA trust in integration.

## Acceptance checks

A real two-team deployment can query known S-41, WS-31 and coverage facts. Re-indexing does not duplicate records. Participant writes and cross-index access fail. Invalid CA fails securely; valid generated CA succeeds. Restart preserves indices and view configuration without Internet.

## Do not expand scope

No second SIEM, live endpoint agents, disabled TLS verification or assumed Elasticsearch/OpenSearch API interchangeability.

## Finish and stop

Review: **standard**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
