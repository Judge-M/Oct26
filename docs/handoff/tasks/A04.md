# A04 — Specify deployment receipts, locking and provider interfaces

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

A03

## Read first

- `ridge/state.py`
- `ridge/storage.py`
- `ridge/export_run.py`
- `proposed ridge/deploy configuration module`
- `EXECUTION.md`

## Implement

Implement private deployment journal primitives separately from the event scoring DB. Define create/probe/resume/stop/inventory provider operations and an event-scoped lock. Persist resource IDs, configuration/release fingerprints, step attempts and verified completion atomically. Define explicit failure/rollback boundaries and partial-run cleanup contracts. Supply a fake provider for deterministic tests only.

## Acceptance checks

Two concurrent up attempts serialize. Kill/restart between create and acknowledgment reconciles one resource instead of creating two. A changed release/profile requires an explicit migration/new-run action. Corrupt or half-written journals fail closed. Tests prove fake receipts cannot count as deployment acceptance.

## Do not expand scope

Do not build a generic workflow engine or implement AWS resources in this task.

## Finish and stop

Review: **focused architecture review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
