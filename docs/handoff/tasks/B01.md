# B01 — Automate and test IRIS bootstrap

## Context and constraints

Repository: https://github.com/Judge-M/Oct26. Planning baseline: main 305c257, reviewed 2026-09-16. Ten teams / thirty participants; Linux shared desktops; local Windows/Hyper-V and AWS; all fictional content published; secrets/live state private. Existing native capture, prepared Autopsy case and desktop are already published. Read ../EXECUTION.md for the common contract. New deployment commands in the plan are proposed, not implemented.

This card is a bounded implementation assignment for a future authorized turn. Do not execute just because this planning file exists. Use the then-current main and reconcile any changes after the planning baseline.

## Prerequisites

A03, A04

## Read first

- `integrations/iris_silent_ridge.py`
- `deployment/expanded/Dockerfile.iris`
- `deployment/expanded/compose.central.yaml`
- `ridge/preflight.py`
- `docs/expanded-deployment.md`

## Implement

Build/run the pinned IRIS image with its real dependencies. Add a bootstrap operation that initializes receipts, creates/finds the exercise case, service user and restricted team users, discovers actual open/closed status IDs, and returns names/IDs to runtime inventory. Use supported APIs or a version-pinned application command with explicit transactions; no guessed numeric values.

## Acceptance checks

Run twice against a disposable real database: one case/user per desired identity, receipt table available, preflight resolves expected names. Post one task and finding through the adapter. A participant cannot mutate native tasks directly or access another case/admin function. Wrong version/schema fails clearly.

## Do not expand scope

No public default credentials; no unsupported database patching without a pinned tested contract.

## Finish and stop

Review: **focused adapter review**. Return the receipt specified in EXECUTION.md with changed files, commit, exact tests, sanitized evidence and limitations. If a prerequisite is missing, name the blocked contract and complete only independent source/test work. Do not repeatedly ask the organizer routine implementation questions or claim success without the specified evidence.
